.git
.idea
.vscode
.cpcache
target
out
.shadow-cljs
.cljs_node_modules
node_modules
dist

# large / irrelevant local files
*.log
*.tmp

# keep docker context small
test
test-resources
coverage
test-results

# Allow generated artifacts in image build, but we generate them in Docker anyway.
#
# Keep package-lock.json in the context so Docker uses the reviewed dependency
# graph committed by the repository.
resources/public/js
src/java/samuraibff/proto
